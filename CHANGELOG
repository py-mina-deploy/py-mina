# 0.1.13/0.1.14

* fix `os.path.join` when deploying from Windows to Linux distro


# 0.1.11

* (conifg) `ask_unlock_if_locked` - prompts to continue if lockfile exists
* (conifg) protected shared files/dirs (protected_shared_files, protected_shared_dirs)
* (conifg) protected files/dirs owner (protected_owner_user, protected_owner_group)
* (conifg) explicit definition of pubilc files ownership (owner_user, owner_group)
* updated `pi_mina init` generator

# 0.1.8

* py_mina.cli init command staged attribute (`--staged`) splits deployfile into `runner & config` logic
  * `py_mina --staged init`

# 0.1.7

* delete all builds from $deploy_to/tmp after deploy
* fix error showing

# 0.1.6

* fix typos
* showing full folders paths (instead of `.` show `/path/to`)
* expose `force_unlock` subtask

# 0.1.5

* tasks execution refactoring
* update docs

# 0.1.4

* fix CLI bug - list tasks

# 0.1.3

* Python 2 compatibility

# 0.1.1

* py_mina cli - added `list` command

# 0.1.0

* py_mina cli

# 0.0.9

* copy `__name__` and `__doc__`from decorated function to decoration function
